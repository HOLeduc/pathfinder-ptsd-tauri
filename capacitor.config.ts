import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pathfinder.ptsdcoach',
  appName: 'Pathfinder PTSD Coach',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;

